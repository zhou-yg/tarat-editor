//
// @@tarat generated file
// provide by @tarat
// warning: auto generated by tarat.do not modifed this file
//
// original writing schema
datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

generator client {
  provider        = "prisma-client-js"
  output          = "./customPrismaClient/client"
  previewFeatures = ["fullTextSearch", "fullTextIndex"]
}

// auto composing schema 
model TodoItemX {
  id         Int      @id @default(autoincrement())
  createdAt  DateTime @default(now())
  modifiedAt DateTime @default(now())

  status      String?
  description String?
}

model Tarat_cascading_list_Folder {
  id         Int      @id @default(autoincrement())
  createdAt  DateTime @default(now())
  modifiedAt DateTime @default(now())

  rm    Boolean                     @default(false)
  name  String
  items Tarat_cascading_list_Item[]
}

model Tarat_cascading_list_Item {
  id         Int      @id @default(autoincrement())
  createdAt  DateTime @default(now())
  modifiedAt DateTime @default(now())

  folder   Tarat_cascading_list_Folder? @relation(references: [id], fields: [folderId])
  folderId Int?

  name String
}

model Tarat_file_uploader_UploaderItem {
  id         Int      @id @default(autoincrement())
  createdAt  DateTime @default(now())
  modifiedAt DateTime @default(now())

  name String
  link String
}

model Tarat_markdown_editor_Markdown {
  id         Int      @id @default(autoincrement())
  createdAt  DateTime @default(now())
  modifiedAt DateTime @default(now())

  title   String?
  content String? // equal sqlite.TEXT 
}

model Tarat_user_login_system_User {
  id         String   @id @default(cuid())
  createdAt  DateTime @default(now())
  modifiedAt DateTime @default(now())

  name     String                                                    @unique
  password String
  avatar2  Tarat_user_login_system_Tarat_file_uploader_UploaderItem?
}

model Tarat_user_login_system_SessionStore {
  id         Int      @id @default(autoincrement())
  createdAt  DateTime @default(now())
  modifiedAt DateTime @default(now())

  fromIndex String // paltform unique cookie id
  userId    String
}

model Tarat_user_login_system_Tarat_file_uploader_UploaderItem {
  id         Int      @id @default(autoincrement())
  createdAt  DateTime @default(now())
  modifiedAt DateTime @default(now())

  name   String
  link   String
  user   Tarat_user_login_system_User @relation(fields: [userId], references: [id])
  userId String                       @unique
}
